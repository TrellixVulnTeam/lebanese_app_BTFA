# this will be the top-most widget of the application
# name this file my.kv


#from lebanese import breakdown_lebanese
#:import breakdown_lebanese lebanese.breakdown_lebanese

#from breakdown_lebanese import arabic_break_down
#:import arabic_break_down breakdown_lebanese.arabic_break_down


#from lebanese import lebanese_to_roman
#:import lebanese_to_roman lebanese.lebanese_to_roman

#from lebanese_to_roman import arabic_to_roman_replace
#:import arabic_to_roman_replace lebanese_to_roman.arabic_to_roman_replace

#:import get_word_breakdown lebanese_to_roman.get_word_breakdown

#from arabic_reshaper import arabic_reshaper
#:import arabic_reshaper arabic_reshaper.arabic_reshaper

#import algorithm bidi.algorithm
#:import get_display bidi.algorithm.get_display

#from bidi import algorithm
#from algorithm import get_display

#:import get_mirrored_arabic lebanese.arabic_direction.get_mirrored_arabic


FloatLayout:
    font_name: 'ariel'
    # here is your background
    Image:
        source: 'C:/Users/natha/PycharmProjects/translator/venv/images/background.png'
    # here you can add other widgets and place their positions, like so:
    Button:
        id: arabic_convert_button
        text: 'Convert!'
        on_release:
            print('test from the button!')
            arabic_output.text = breakdown_lebanese.arabic_break_down(arabic_input.text) + press_enter.text
            arabic_output.text += lebanese_to_roman.arabic_to_roman_replace(arabic_input.text) + equals.text + arabic_input.text + press_enter.text
            arabic_output.text += lebanese_to_roman.get_word_breakdown(arabic_input.text) + press_enter.text
            arabic_output.text += lebanese_to_roman.get_new_roman_string(arabic_input.text) + press_enter.text
            #some_label.text = ' button pushed!'
        size_hint: .2, .3
        pos_hint: {'center_x': .5, 'center_y': .5}
    Label:
        id: some_label
        text: 'Characters'
        size_hint: 1, .2
        font_size: '16dp'
        pos_hint: {'x': -0.45, 'y': 0.19}
        color: [0,0,1,1]

    Label:
        id: arabic_label
        text: "Input arabic word to have it translated: "
        size_hint: 1, .2
        font_size: '24dp'
        pos_hint: {'x': 0, 'y': .8}
        font_name: 'arial'
    TextInput:
        id: arabic_input
        size_hint: .8, .2
        pos_hint: {'center_x': .5, 'center_y': .78}
        multiline: True
        text: ''
        on_text:
            arabic_input.text = get_mirrored_arabic(arabic_input.text)
            #arabic_reshaper.reshape(arabic_input.text)
            #bidi_text = bibi.algorithm.get_display(arabic_input.text)
        font_size: '22dp'
        font_name: 'arial'

    TextInput:
        id: arabic_output
        size_hint: .8, .2
        pos_hint: {'center_x': .5, 'center_y': .22}
        multiline: True
        text: ''
        font_size: '22dp'
        font_name: 'arial'
          #  if len(test_word) != None:
           #     text = arabic_input
        #on_text_validate:

         #   test_word
          #  #arabic_label.text = ' TESTING DUDE!'
    Label:
        id: press_enter
        text: '\n'
        size_hint: 0, 0.1
    Label:
        id: equals
        text: ' = '
        size_hint: 0,0.2


